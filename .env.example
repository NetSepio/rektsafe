# Reown AppKit Project ID
# Get yours at: https://cloud.reown.com
NEXT_PUBLIC_REOWN_PROJECT_ID=your_project_id_here

# Optional: Custom Solana RPC endpoint (defaults to mainnet)
# NEXT_PUBLIC_SOLANA_RPC=https://api.mainnet-beta.solana.com

# Helius RPC for zkSend private balance fetching
# Get yours at: https://helius.xyz
NEXT_PUBLIC_HELIUS_RPC=https://mainnet.helius-rpc.com/?api-key=your_api_key

# ============================================
# zkVault - NFT Lending Vault Configuration
# ============================================

# Multisig-controlled vault wallet address for receiving deposits
# This is the address users send USDC/USDT to
NEXT_PUBLIC_VAULT_ADDRESS=your_multisig_vault_address_here

# Supabase Configuration (Optional - uses mock storage if not set)
# Used for storing withdrawal requests, yield claims, and position data
# Get yours at: https://supabase.com
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here

# Supabase Anon Key (Public/Publishable Key)
# Found in: Supabase Dashboard > Project Settings > API > Project API keys
# This is the "anon public" key - safe to expose in browser
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Note: The Anon Key above IS your publishable key for client-side operations.
# You do NOT need a separate publishable key for Supabase.
#
# The Service Role Key (starts with "eyJ...") should NEVER be exposed in
# client-side code - it's for server-side admin operations only.
# Since this is a static export app, we use mock storage or edge functions
# for any admin operations that require elevated permissions.

# Optional: Base path for GitHub Pages deployment
# Leave empty for custom domains like rektsafe.com
# BASE_PATH=""
